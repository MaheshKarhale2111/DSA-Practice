#include <bits/stdc++.h>
using namespace std;

class node
{

public:
    int data;
    node *next;
    node(int x)
    {
        data = x;
        next = NULL;
    }
};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    return 0;
}